package it.poste.anc.workflow.engine;

import org.springframework.context.annotation.Configuration;

/**
 * Configurazione foundation Flowable embedded per Sprint 0.
 * In baseline locale mantiene solo un placeholder tecnico non bloccante:
 * la configurazione specifica Flowable viene gestita dall'autoconfigurazione quando disponibile.
 */
@Configuration
public class FlowableEngineConfig {
}
