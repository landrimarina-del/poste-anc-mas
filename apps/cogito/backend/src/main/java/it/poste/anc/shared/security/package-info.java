/**
 * Modulo M-IAM (autenticazione + RBAC).
 * Sprint 0: Basic Auth + sessione, lettura ruoli/gruppi da tabella app_user.
 * Nel target si sostituisce con OIDC/Keycloak senza modificare le API (cap. 05 Architect).
 */
package it.poste.anc.shared.security;
